{
  "name": "node-app",
  "version": "1.0.0",
  "scripts": {
    "test": "jest",
    "build": "mkdir -p dist && echo \"Build successful\" > dist/output.txt"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
